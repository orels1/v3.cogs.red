const API = 'https://cogs.red/api/v1/';
const COGS = `${API}cogs`;
const REPOS = `${API}repos`;
const TAGS = `${API}misc/tags/top`;
const SEARCH = `${API}search/cogs`;

export default {
  API,
  COGS,
  REPOS,
  TAGS,
  SEARCH,
};
